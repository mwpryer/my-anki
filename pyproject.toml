[project]
name = "cantonese"
version = "0.1.0"
description = "Build Anki decks from Cantonese textbook content"
requires-python = ">=3.13"
dependencies = [
    "genanki>=0.13.1",
    "pycantonese>=4.2.0",
    "pydub>=0.25.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
